## Packages
leaflet | Core mapping library
react-leaflet | React components for Leaflet
@types/leaflet | TypeScript definitions for Leaflet

## Notes
The application uses OpenStreetMap tiles which do not require an API key.
Leaflet CSS will be imported in the main entry point or index.css.
Authentication is handled via Replit Auth (blueprint:javascript_log_in_with_replit).
